mlops/api/requirements.txt
Python dependencies for the MLOps API and Training script.
Use 'pip install -r requirements.txt' to install them.
For the ML framework and tracking

mlflow>=1.20.0
scikit-learn>=1.0.0
For the API server

fastapi>=0.70.0
uvicorn>=0.15.0
For data manipulation

numpy>=1.20.0
Pydantic is a dependency of FastAPI, but it's good practice
to specify it for clarity.

pydantic>=1.9.0
